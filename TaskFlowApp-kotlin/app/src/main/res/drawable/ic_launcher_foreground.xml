<?xml version="1.0" encoding="utf-8"?>
<!--
    Placeholder launcher foreground: a checkmark on the adaptive-icon canvas.

    Adaptive icons use a 108x108dp viewport of which only the centre 72x72dp is
    guaranteed visible (launchers mask the rest), so the mark is kept well
    inside that safe zone.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path
        android:pathData="M36,55.5 L48.5,68 L72.5,40"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="8"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
